version: '3.8'

services:
  gemini-chat:
    build: .
    ports:
      - "8501:8501"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    env_file:
      - .env
    volumes:
      - ./app.py:/app/app.py
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0
